from typing import Protocol, Optional

class PartitionStrategy(Protocol):
    def parent_clause(self, column: str) -> str: ...
    def child_clause(self, row: dict, schema: str, table: str) -> Optional[str]: ...
