https://skwrites.in/setting-up-apache-airflow-cluster/
https://blogs.halodoc.io/airflow-cluster-architecture-at-halodoc-self-managed-challenges-and-learnings/
https://medium.com/softwaresanders/setting-up-an-apache-airflow-cluster-60fbd8a93276

sudo apt-get update
sudo apt-get upgrade
sudo apt-get install -y make build-essential libssl-dev zlib1g-dev        libbz2-dev libreadline-dev libsqlite3-dev wget curl llvm        libncurses5-dev libncursesw5-dev xz-utils tk-dev
wget https://www.python.org/ftp/python/3.8.4/Python-3.8.4.tgz

tar xvf Python-3.8.4.tgz
cd Python-3.8.4
./configure --enable-optimizations --with-ensurepip=install --prefix=$HOME/python3.8
make -j 8
make install
/home/t610959/python3.8/bin/python3 -m venv airflow_env
cd airflow_env/
source bin/activate
export AIRFLOW_HOME=~/airflow
AIRFLOW_VERSION=2.3.2
PYTHON_VERSION="$(python --version | cut -d " " -f 2 | cut -d "." -f 1-2)"
CONSTRAINT_URL="https://raw.githubusercontent.com/apache/airflow/constraints-${AIRFLOW_VERSION}/constraints-${PYTHON_VERSION}.txt"
pip install "apache-airflow==${AIRFLOW_VERSION}" --constraint "${CONSTRAINT_URL}"
airflow db init
pip install 'apache-airflow[crypto]'
pip install 'apache-airflow[celery]'
pip install 'apache-airflow[password]'
